#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", SB.Mean.Peak.Amplitude = "Small bowel contractions"),
missing_text = "Missing"
)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, SB.Mean.Peak.Amplitude),
label = list(SB.Time = "Small bowel transit time (hours)", SB.Mean.Peak.Amplitude = "Small bowel contractions"),
missing_text = "Missing"
) %>%
#add p-value
add_p()
view(smartpill)
library(tidyverse)
library(gtsummary)
library(medicaldata)
smartpill <- smartpill %>%
mutate(Group = as.factor(Group))
summary(smartpill)
tbl_summary(smartpill)
smartpill %>%
tbl_summary(
include = c(Group, Age, SB.Time, WG.Time)
)
smartpill %>%
tbl_summary(
include = c(Group, Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
)
)
smartpill %>%
tbl_summary(
include = c(Group, Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Small bowel contractions"),
missing_text = "Missing"
)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Small bowel contractions"),
missing_text = "Missing"
)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Small bowel contractions"),
missing_text = "Missing"
) %>%
#add p-value
add_p()
library(tidyverse)
library(gtsummary)
library(medicaldata)
smartpill <- smartpill %>%
mutate(Group = as.factor(Group))
summary(smartpill)
tbl_summary(smartpill)
smartpill %>%
tbl_summary(
include = c(Group, Age, SB.Time, WG.Time)
)
smartpill %>%
tbl_summary(
include = c(Group, Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
)
)
smartpill %>%
tbl_summary(
include = c(Group, Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, SB.Contractions),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", SB.Contractions = "Small bowel contractions"),
missing_text = "Missing"
)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value
add_p()
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test (here, a t-test)
add_p(test = Age ~ "t.test")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
tab_footnote(footnote = "TEST")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt()
tab_footnote(footnote = "TEST")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt() %>%
tab_footnote(footnote = "TEST")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt()
test <- smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt()
test %>% tab_footnote(footnote = "TEST")
library(gt)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt() %>%
tab_footnote(footnote = "TEST")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt() %>%
#remove existing footnotes
include = -tab_footnote() %>%
tab_footnote(footnote = "TEST")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt() %>%
#remove existing footnotes
include = -tab_footnote() %>%
tab_footnote(footnote = "TEST",
#which column should have footnote
locations = cells_column_labels(columns = vars(p.value)))
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object
as_gt() %>%
#remove existing footnotes
include = -tab_footnote() %>%
tab_footnote(footnote = "Tests used are...",
locations = cells_column_labels(columns = vars(p.value)))
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object and remove existing footnotes
as_gt(include = -tab_footnote()) %>%
tab_footnote(footnote = "Tests used are...",
locations = cells_column_labels(columns = vars(p.value)))
library(gt)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object and remove existing footnotes
as_gt(include = -tab_footnote()) %>%
tab_footnote(footnote = "Tests used are...",
locations = cells_column_labels(columns = vars(p.value)))
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object and remove existing footnotes
as_gt() %>%
#remove existing footnotes
include = -tab_footnote()
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object and remove existing footnotes
as_gt() %>%
#remove existing footnotes
include = -tab_footnote %>%
tab_footnote(footnote = "Tests used are...",
locations = cells_column_labels(columns = vars(p.value)))
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object and remove existing footnotes
as_gt(include = -tab_footnote) %>%
tab_footnote(footnote = "Tests used are...",
locations = cells_column_labels(columns = vars(p.value)))
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object keeping everything except the existing footnotes
as_gt(include = -tab_footnote) %>%
tab_footnote(footnote = "t.test for age; Wilcox rank sum for others",
locations = cells_column_labels(columns = vars(p.value)))
show_header_names(test)
test <- smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test")
show_header_names(test)
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, SB.Contractions),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", SB.Contractions = "Small bowel contractions"),
missing_text = "Missing"
) %>%
#show existing column headers of this table
show_header_names()
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#show existing column headers of this table
show_header_names()
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, SB.Contractions),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", SB.Contractions = "Small bowel contractions"),
missing_text = "Missing"
) %>%
#modify column headers
modify_header(label = "Variable", stat_1 = "Critically Ill", stat_2 = "Healthy")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#modify column headers
modify_header(label = "Variable", stat_1 = "Critically Ill", stat_2 = "Healthy")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#modify column header text and make bold with **
modify_header(label = "**Variable**", stat_1 = "**Critically Ill**", stat_2 = "**Healthy**")
Table1 <- smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#modify column header text and make bold with **
modify_header(label = "**Variable**", stat_1 = "**Critically Ill**", stat_2 = "**Healthy**")
Table1
Table1 %>%
#add p-value
add_p()
Table1
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test")
smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test")
Table1 %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
#need to indicate grouping variable or will get error
add_p(test = Age ~ "t.test", group = Group)
Table1 %>%
#add p-value, override default to specify which test to use for Age (here, a t-test)
#need to indicate grouping variable, even though set in Table1, or will get error
add_p(test = Age ~ "t.test")
Table1
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object keeping everything except the existing footnotes
as_gt(include = -tab_footnote) %>%
tab_footnote(footnote = "t.test for age; Wilcox rank sum for others",
locations = cells_column_labels(columns = vars(p.value)))
Table1 <- smartpill %>%
mutate(Group = as.factor(Group)) %>%
tbl_summary(
by = Group,
include = c(Age, SB.Time, WG.Time),
statistic = list(
#change just way continuous vars are presented
all_continuous() ~ "{median} ({min}, {max})"
),
label = list(SB.Time = "Small bowel transit time (hours)", WG.Time = "Whole gut time (hours)"),
missing_text = "Missing"
) %>%
#modify column header text and make bold with **
modify_header(label = "**Variable**", stat_1 = "**Critically Ill**", stat_2 = "**Healthy**")
Table1
Table1
#add p-value, override default to specify which test to use for Age (here, a t-test)
add_p(test = Age ~ "t.test") %>%
#convert to gt object keeping everything except the existing footnotes
as_gt(include = -tab_footnote) %>%
tab_footnote(footnote = "t.test for age; Wilcox rank sum for others",
locations = cells_column_labels(columns = vars(p.value)))
